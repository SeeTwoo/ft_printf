#include <stddef.h>
//#include "bigass_string.h"
#include <stdio.h>

int	ft_printf(char const *format, ...);

int	main(void) {
	ft_printf("%s %s %s\n", "one", "and", "two"); 
	ft_printf("%");
	return (0);
}
